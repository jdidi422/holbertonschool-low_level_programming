#include "main.h"

/**
  * print_numbers - Prints numbers from 0 to 9 followed by a new line - print numbers Do not print 2 and 4
 */
void print_numbers(void)
{
	char print_numbers;

	for (print_numbers = '0'; print_numbers <= '9'; print_numbers++)
		{
        if (i != 2 && i != 4) {
		 }

	{
		_putchar(print_numbers);
	}
	_putchar('\n');
}

